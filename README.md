# mmrouter

Intelligent LLM request routing. Classify prompts, route to the optimal model, cut costs 40-70%.

## Install

```bash
pip install -e ".[dev]"
```

## Usage

```bash
mmrouter route "What is the capital of France?"
mmrouter classify "Explain quantum computing"
```
